spring:
  ai:
    mcp:
      server:
        stdio: true
        name: order-service
        version: 1.0.0
  main:
    web-application-type: none
    banner-mode: "off"

logging:
  threshold:
    console: OFF
  file:
    name: /tmp/order-service-stdio.log