spring:
  ai:
    mcp:
      server:
        name: order-service
        version: 1.0.0
        protocol: STREAMABLE
        request-timeout: 90s
  application:
    name: order-service

  datasource:
    url: jdbc:h2:mem:orders
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  h2:
    console:
      enabled: true

  main:
    banner-mode: "off"

server:
  port: 8080

logging:
  level:
    root: WARN
    com.themcpguy: INFO
    org.springframework.ai.mcp: INFO
